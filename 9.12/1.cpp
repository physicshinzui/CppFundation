
#include <iostream>
 
int main()
{
    // shinji: an array declared whose elements are char type. And the length is one and it posesses the string Alex. 
    char myName[]{ "Alex" }; // fixed array
    std::cout << myName << '\n';
 
    return 0;
}